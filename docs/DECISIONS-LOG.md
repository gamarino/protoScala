# Decisions log

Decisions taken during implementation, with who took them. Entries were marked
**[agent, pending review]** when taken by the implementing agent under the
maintainer's authorisation of 2026-09-22 ("decide what is missing and correct
it tomorrow").

**Review of 2026-09-23.** The maintainer has now ruled on every one of them.
The original wording of each decision is kept as it was written; the ruling is
recorded beside it. Four outcomes are not a plain approval:

- **D47 overturned** — `Future.apply` now takes its body **by name**,
  `Future(expr)`. Ruling: *least surprise for the Scala programmer*. By-name
  parameters were implemented as a general language feature to make it possible,
  with the new deviation **D53** for the call sites the compiler cannot resolve.
- **D45 overturned** — an actor handler may return a bare `newState` as well as
  `(newState, reply)`; with no reply the ask's future completes with `()`. Same
  ruling: forcing a handler that only updates state to invent a reply is the
  surprise to remove.
- **D44 superseded** — rather than leaving Phases 3 and 4 unbuilt, the maintainer
  directed that both be completed. The version implication of D44 (Phase 5
  shipping as 0.3.0 ahead of them) is to be revisited when they land.
- **D46 stands as approved**, with its stated precondition now met: it deferred
  unanchoring "until `ProtoMap` lands", and `ProtoMap` shipped in protoCore
  2.0.0 and is merged and released. It is ready to revisit; the behaviour is
  unchanged for now.

Two entries are **acknowledged, not approved as done**: ThreadSanitizer was
never run against the actors, and the cold-start budget is not claimed as met.
They stay visible as known gaps.

Two items remain **open for the maintainer** — see "Still open" at the end of
this file.

| Date | Decision | Taken by | Where |
|---|---|---|---|
| 2026-09-22 | Scala 3 complete syntax (braces + indentation); types parsed and erased | maintainer | DESIGN §2 |
| 2026-09-22 | `List` on `ProtoList` (AVL) | maintainer | DESIGN §6 |
| 2026-09-22 | `super` in stackable traits resolved in protoScala (O(n) walk) | maintainer | DESIGN §4.4 |
| 2026-09-22 | Recursive VM with frame snapshots for cooperative `await` | maintainer | DESIGN §3.6, §8.3 |
| 2026-09-22 | Map/Set on a new protoCore type `ProtoMap` (GC-traced keys) | maintainer | PROTOMAP-SPEC |
| 2026-09-22 | Actor model on protoClojure's design; mailboxes on a new protoCore type `ProtoMPSCQueue` | maintainer | DESIGN §8, PMQ-SPEC |
| 2026-09-22 | P1: iterator option (a); hashed-collection helper in protoCore | maintainer (accepted recommendation) | PROTOMAP-SPEC §3.3, §4 |
| 2026-09-22 | Overnight scope: Phases 1+2 on protoScala `main`; P1 on a protoCore branch, not merged, embedders not rebuilt | maintainer | — |
| 2026-09-22 | P1 D2a–D7 (bucket encoding, hashed key word, keep key, nullptr keys, isEqual identity, own prototype, CellType discrimination, SOVERSION 1) | agent; approved by the maintainer on 2026-09-23 | PROTOMAP-SPEC §7 |
| 2026-09-22 | Phase 1 Q1–Q22: the plan's provisional behaviours adopted as written (notably Q21: `ProtoContext::safepoint()` at loop back-edges, touching DESIGN R1; Q4: captured `var`s boxed in mutable cells; Q2: script mode with eager top-level vals); recorded as provisional D9–D18 in STATUS.md by Task 13 | agent; approved by the maintainer on 2026-09-23 | plans/2026-09-22-phase-1-core-language.md, "Open questions for the maintainer" |
| 2026-09-22 | Rename `ProtoSparseListObject` → **`ProtoMap`** (and iterator, tag, CellTypes, accessors `newMap`/`isMap`/`asMap`, prototype `mapPrototype`, files, spec) | maintainer | protoCore feature/pslo-p1, docs |
| 2026-09-23 | Phase 2 plan Q1–Q14: the plan's provisional behaviours adopted as written; D28–D34 recorded as provisional | agent; approved by the maintainer on 2026-09-23 | plans/2026-09-22-phase-2-object-model.md |
| 2026-09-23 | D35–D42 added to the provisional Phase 2 list; they were collected while implementing the phase and answer no numbered question: a refutable generator without `case` filters (D35), for-comprehension desugaring differences that leave the result unchanged (D36), intersection types cannot be tested at run time (D37), the default `toString` shape (D38), `List` hash codes (D39), the `override`-on-`var` diagnostic wording (D40), no universal apply for a plain class (D41), `MatchError` names the Scala class (D42). D41 and D42 were described in tutorial chapter 3 without an id; D41 also qualified the STATUS "universal apply" row, which had read as an over-claim | agent; approved by the maintainer on 2026-09-23 | docs/STATUS.md, docs/LANGUAGE.md §5, docs/tutorial/03-*.md |
| 2026-09-23 | The ledger's "compound-type pattern" deviation re-derived and **withdrawn**. The original note claimed protoScala accepts `case x: A & B` where scalac rejects it; measured against scalac 3.9, *both* reject the unparenthesised form, and the real divergence is the opposite one — scalac accepts `case x: (A & B)` and `x.isInstanceOf[A & B]` and tests both components, protoScala rejects them at compile time. Recorded as D37 with fixtures | agent; approved by the maintainer on 2026-09-23 | docs/STATUS.md D37, tests/conformance/11-pattern-matching/intersection-type-pattern*.scala |
| 2026-09-23 | Brace/indentation fixture pairing closed for the Phase 2 directories (`07-classes` … `12-for-comprehensions`): every fixture whose syntax differs now has a `-braces` twin (101 new fixtures, all green). The Phase 1 directories (`01-lexical` … `06-recursion`) and `tutorial/` keep representative pairs only — a tutorial fixture must mirror a published snippet verbatim, so a braces twin would have no snippet to mirror | agent; approved by the maintainer on 2026-09-23 | tests/conformance/, .superpowers/.../constraints.md |
| 2026-09-23 | Release 0.2.0 cut; the `v0.2.0` tag is left to the maintainer | agent (per plan) | CMakeLists.txt, CHANGELOG.md |
| 2026-09-23 | Phase 5 A0-1: cooperative `await` suspends the recursive VM through **per-frame catch-and-prepend snapshots** (option (a)); the VM stays recursive | agent; approved by the maintainer on 2026-09-23 | plans/2026-09-23-phase-5-actors.md A0-1, src/runtime/ExecutionEngine.cpp |
| 2026-09-23 | Phase 5 A0-2: an `await` whose call chain cannot be snapshotted raises `UnsupportedOperationException` instead of blocking a worker or re-running side effects; recorded as **D43** | agent; approved by the maintainer on 2026-09-23 | A0-2, docs/STATUS.md D43 |
| 2026-09-23 | Phase 5 A0-3: Phase 5 ships **before** Phases 3 and 4, so the release is **0.3.0** and the prelude gains `Try`/`Success`/`Failure` and `RuntimeError(className, message)` early; recorded as **D44** | agent; **superseded by the maintainer on 2026-09-23**: Phases 3 and 4 are to be completed rather than left unbuilt, and D44's version implication is to be revisited when they land | A0-3, lib/prelude.scala |
| 2026-09-23 | Phase 5 A0-4: an actor handler must return **`(newState, reply)`** strictly; anything else raises `IllegalArgumentException` and fails that message; recorded as **D45** | agent; **overturned by the maintainer on 2026-09-23** (least surprise for the Scala programmer): a handler may also return a bare `newState`, and the ask's future then completes with `()`. A `Tuple2` is still read as the pair form; only a handler that produces no value at all is rejected | A0-4, src/runtime/ActorScheduler.cpp |
| 2026-09-23 | Phase 5 A0-5: an actor is **anchored for the session** in a registry pinned in a root slot (no unanchoring until `ProtoMap` lands); recorded as **D46** | agent; approved by the maintainer on 2026-09-23. For the record, its stated precondition is met: `ProtoMap` shipped in protoCore 2.0.0 and is merged and released, so unanchoring is ready to revisit. The behaviour is unchanged | A0-5, src/runtime/Runtime.cpp |
| 2026-09-23 | Phase 5 A0-6: `Future.apply` takes a **function**, not a by-name parameter — `Future(() => expr)`; recorded as **D47** | agent; **overturned by the maintainer on 2026-09-23** (least surprise for the Scala programmer): `Future.apply` takes its body by name — `Future(expr)` and `Future { … }`. By-name parameters were implemented as a general feature to make it possible; the call sites that cannot be resolved are the new deviation **D53** | A0-6, src/compiler/Compiler.cpp, src/runtime/ActorPrimitives.cpp |
| 2026-09-23 | Phase 5 A0-7: `map`/`flatMap`/`recover`/`onComplete` continuations run **on the thread that completes the future** (or immediately on the caller when it is already complete); recorded as **D48** | agent; approved by the maintainer on 2026-09-23 | A0-7, src/runtime/Futures.cpp |
| 2026-09-23 | Phase 5 A0-8: the actor mailbox is consumed through a **`Mailbox` seam** — `ProtoMPSCQueue` when the linked protoCore provides it, a CAS'd `ProtoList` otherwise; protoCore 2.0.0 has no `newMPSCQueue`, so 0.3.0 ships the fallback | agent; approved by the maintainer on 2026-09-23 | A0-8, src/runtime/Mailbox.cpp, CMakeLists.txt |
| 2026-09-23 | Phase 5 A0-9: the benchmarks need a clock and non-worker sender threads, so `Thread.start`/`join` and `System.nanoTime`/`currentTimeMillis`/`getenv` are added as runtime facilities; recorded as **D49** | agent; approved by the maintainer on 2026-09-23 | A0-9, src/runtime/ActorPrimitives.cpp |
| 2026-09-23 | Phase 5 A0-10: a turn drains a **batch of 8** messages (the DESIGN §8.2 value), with the remainder of a `takeAll` kept in a per-band `__pend<n>__` list on the actor | agent; approved by the maintainer on 2026-09-23 | A0-10, src/runtime/ActorScheduler.cpp |
| 2026-09-23 | Phase 5 A0-11: **no new opcodes**; `!`, `?`, `await` and friends are ordinary sends to native methods and the 128..159 range stays reserved | agent; approved by the maintainer on 2026-09-23 | A0-11, src/compiler/Opcodes.h |
| 2026-09-23 | Phase 5 deviations **D43–D52** recorded as provisional in STATUS.md, LANGUAGE.md §5 and tutorial chapter 3 | agent; approved by the maintainer on 2026-09-23, with D45 and D47 replaced by the rulings above and **D53** added | docs/STATUS.md, docs/LANGUAGE.md, docs/tutorial/03-*.md |
| 2026-09-23 | Plan Tasks 3–7 were committed as **one** commit rather than five: the handler calling convention, the ask future and the `FutureYield` catch are the same code path, and splitting them would have produced intermediate commits that do not build | agent; approved by the maintainer on 2026-09-23 | commit "actors: scheduler, Scala surface, futures and cooperative await" |
| 2026-09-23 | Two bugs found by the tests and fixed against the plan's own pseudocode: (a) `Futures::complete` wrote the value **before** the state CAS, so a losing completer overwrote the winner's value — replaced by a claiming CAS (`kClaiming`, still read as pending) followed by the write and the final store; (b) `awaitBlocking` read the wake epoch *inside* the lock, after the state check, losing every wake-up and charging the 50 ms safety net to every blocking ask (a `priority` run went from 7.4 s to 0.11 s once the epoch was read before the state) | agent; approved by the maintainer on 2026-09-23 | src/runtime/Futures.cpp |
| 2026-09-23 | ThreadSanitizer (plan Task 8 Step 4) **not run**: the host was shared with three other builds all night, and a TSan run would have invalidated the benchmark table measured on it. Recorded in STATUS as not claimed | agent; **acknowledged by the maintainer on 2026-09-23 as a known gap, not approved as done**: the TSan run is still owed | docs/STATUS.md "Open bugs" |
| 2026-09-23 | A **P1 rooting bug in the plan's own `Mailbox::push` pseudocode** was found and fixed: the `__items__` snapshot was held only in a C++ local across `appendLast`, which allocates, so a producer that lost the compare-and-swap kept walking a list the collector could already reclaim (SIGSEGV in `implInsertAt` under a heap limit with 8 senders). Every compare-and-swap snapshot in the phase is now rooted in an automatic-local slot before the allocation: `Mailbox::push`, `Mailbox::takeAll`, the actor registry, the `Thread.start` registry, `futures::addWaiter`, `futures::onComplete`, `runContinuations`, and the batch in `nextMessage` | agent; approved by the maintainer on 2026-09-23 | src/runtime/Mailbox.cpp, ActorScheduler.cpp, Futures.cpp, ActorPrimitives.cpp |
| 2026-09-23 | `Actor.stats` now counts a message **before** the handler runs, not after: the ask's future is completed inside `deliver`, so a caller holding a reply could otherwise still read `messagesProcessed == 0` (the tutorial `13-actors-stats` fixture failed at 16 workers) | agent; approved by the maintainer on 2026-09-23 | src/runtime/ActorScheduler.cpp `runTurn` |
| 2026-09-23 | `tests/cli/actors-stress.sh` pins its own `PROTOCORE_HEAP_LIMIT_CELLS=8000000`, as `benchmarks/object_tree.scala` already does. Measured: its 200000-message backlog on one actor needs between 1000000 and 1500000 cells with the CAS-list mailbox, so the ambient 20000-cell sweep would report an honest out-of-memory, not a rooting defect | agent; approved by the maintainer on 2026-09-23 | tests/CMakeLists.txt |
| 2026-09-23 | The **cold-start budget (< 25 ms) is not claimed as met**: 24.20 / 23.58 ms at load ~4 but 25.11 / 26.57 ms at load ~8.7 on a shared host. Not eager worker creation (the pool starts at the first `Actor.spawn`; 2 vs 12 `clone3` calls), but the prelude grew with D44's `Try`/`RuntimeError`/`ActorStats`. Re-measure on a quiet host | agent; **acknowledged by the maintainer on 2026-09-23 as a known gap, not approved as met**: the budget is still owed a measurement on a quiet host | docs/STATUS.md |
| 2026-09-23 | Release 0.3.0 cut; the `v0.3.0` tag is left to the maintainer | agent (per plan) | CMakeLists.txt, CHANGELOG.md |

## Overnight run 2026-09-22 — approved by the maintainer on 2026-09-23

Every ruling below is approved as recorded. The two rows that describe work
*not* done (the P1 perf gate and ASan findings, and P1 Task 12) are outstanding
work, not decisions: they stay on the list until they are run.

| Decision | Cost if wrong |
|---|---|
| Phase 1 plan open questions Q1–Q22 adopted as the plan's provisional behaviours (incl. Q21: `safepoint()` at loop back-edges, touching DESIGN R1; Q4: captured vars boxed in mutable cells) | each reversible; Q21 is a GC-interaction choice to confirm with protoCore |
| Layout: a same-line closer (`else`, `catch`, `do`, `yield`) pairs with a pending partner inside the current region before popping it (Scala semantics over the plan's simpler rule) | extra layout complexity |
| Forward references follow SLS 4.4 (interval includes the referenced definition): `{ def f = y; val y = 1 }` is rejected, as in scalac | a program scalac also rejects |
| REPL redefinition uses per-definition keys `x#N` (Scala REPL shadowing); a throwing `val` leaves the name undefined | compiler/global-table change |
| Primitives: negative shift, `Int.toChar` out of range → IllegalArgumentException; `toInt`/`toLong`/`round` of ±Infinity → ArithmeticException; Char predicates ASCII-only (D19–D22) | small behaviour changes |
| `@main` accepts only no parameters or `args: String*` (typed parameters recorded as a deviation) | missing Scala 3 feature |
| P1: perf gate parked (retired instructions flat, cycles noisy under host load) and ASan pre-existing findings (3 timing/limit tests, ProtoSpace teardown leaks) not fixed | a real slowdown could slip; re-run `perf stat -r 3` on a quiet host before merge |
| P1: Task 12 (rebuild of every embedder) not run; branch `feature/pslo-p1` not merged, not pushed | embedder validation pending |

## Phase 2 open questions (2026-09-22)

| Decision | Taken by |
|---|---|
| Q2: class/trait membership needs a new protoCore API (allocation-free walk of the flattened parent chain, no arbitrary step limit), shared by the family; protoScala uses a per-class marker attribute until it lands | maintainer |
| Q1: instances under construction are rebuilt field by field; an early-escaped `this` sees an older version — accepted as deviation D28 | maintainer |
| Q14: Phase 2 writes tutorial chapters 6, 7 and 9 (index numbering kept; 8 = collections in Phase 3) | maintainer |
| Q3–Q13: plan recommendations accepted (immutable class prototypes, compile-time companion links, class-qualified private keys, plain `super` in Phase 2 / `super[T]` in Phase 4, top-level classes only, embedded Scala `Option` prelude, minimal `List` moved up from Phase 3, `SEND_KW` for native named args, representation-based type tests D29, D30/D31, lazy members D34, REPL class shadowing) | maintainer (accepted recommendations) |
| 2026-09-22 | Q2 refined: no new protoCore API; correct `isInstanceOf`/`hasParent` implementations (linear walk of the flattened chain, no step cap, no allocation, same contracts) — docs/platform/ISINSTANCEOF-FIX.md | maintainer |
| 2026-09-22 | `setParents` flattens the chain (explicit parents in order, then missing ancestors appended); `isInstanceOf` becomes a pure linear walk; two pre-existing `isInstanceOf` bugs fixed (non-first parents, mutable snapshots) — embedder suites to run before merge | maintainer |
| 2026-09-22 | protoCore merge (ProtoMap + isInstanceOf/setParents fixes) is versioned **2.0.0 with SOVERSION 2** (ABI and behaviour change); then rebuild, check and fix protoPython, protoJS, protoST, protoClojure and protoScala, and push everything | maintainer |
| 2026-09-22 | protoCore C1: fix the root cause in `newChild` (take the chain from a mutable prototype's current snapshot) rather than patching isInstanceOf; C2: keep the universal-root answer for parentless objects | maintainer |
| 2026-09-22 | protoCore: remove `getAttribute`'s 500-step cap (chains are flat and finite; setParents rejects cycles), so every lookup helper agrees at any depth | maintainer |
| 2026-09-22 | protoCore C2/C3: keep the `newChild` fix; fix the embedders instead — protoJS integrity markers (freeze/seal/preventExtensions) must stop being parent-chain markers, protoPython instances must not inherit the metaclass chain | maintainer |
| 2026-09-22 | Cold-start target raised from < 20 ms to < 25 ms (Phase 2: the embedded Scala prelude adds ~1.2 ms and the standard library will keep growing) | maintainer |
| 2026-09-23 | Closing sequence gains a final step: installers for protoCore + the five runtimes (Linux verified here; macOS/Windows configured but unverified), each runtime depending on an installed protoCore rather than bundling it | maintainer |
| 2026-09-23 | 0.2.0 benchmark numbers are re-measured against protoCore 2.0.0 (the shipped stack) on a quiet machine, not kept tied to e43fa2e4 | maintainer; approved 2026-09-23 |

## Phase I — native installers (2026-09-23) — approved by the maintainer on 2026-09-23

Taken while implementing `docs/plans/2026-09-23-phase-i-installers.md`. Each one
spans the whole family (protoCore plus the five runtimes), not protoScala alone.

| ID | Decision | Taken by |
|---|---|---|
| **D-I1** | protoCore emits a real CMake package config (`install(EXPORT)` + `configure_package_config_file` + `write_basic_package_version_file` with `SameMajorVersion` + an explicit `SOVERSION` assertion) and a pkg-config `.pc`. Every runtime prefers `find_package(protoCore <floor> CONFIG)`; sibling-directory discovery is kept as an explicit developer fallback used only when no installed package is found, and refused by `-DPROTOCORE_REQUIRE_PACKAGE=ON` | agent; approved by the maintainer on 2026-09-23 |
| **D-I2** | The unconditional `DEB;RPM;TGZ` CPack generators in protoPython, protoST, protoClojure and protoJS are guarded on `find_program(dpkg)` / `find_program(rpmbuild)`. `cpack` runs every configured generator in one pass and a missing tool is fatal, so on a host without `rpmbuild` the DEB and the TGZ went down with the RPM | agent; approved by the maintainer on 2026-09-23 |
| **D-I3** | protoScala gains CPack **now** rather than "in Phase 6": the maintainer's scope is all five runtimes. `protoscala-0.3.0-Linux.deb` is this repository's first package | agent; approved by the maintainer on 2026-09-23 |
| **D-I4** | protoJS keeps its standalone `packaging/` pipeline; it is not migrated to CPack. Its hardcoded protoCore `>= 1.0.0` checks are raised to the `[2.0.0, 3.0.0)` range and backed by a `libprotoCore.so.2` soname check | agent; approved by the maintainer on 2026-09-23 |
| **D-I5** | protoST's stdlib self-location gains the macOS (`_NSGetExecutablePath`) and Windows (`GetModuleFileNameA`) branches, ported from protoPython's `getExecutablePath()` | agent; approved by the maintainer on 2026-09-23 |
| **D-I6** | Linux packages are built, installed into a scratch prefix inside the workspace and smoke-tested there. macOS and Windows packaging is configured and reviewed but marked **UNVERIFIED** in the plan and in every repository's installation document | agent; approved by the maintainer on 2026-09-23 |
| **D-I7** | The root-owned protoCore `1.0.0` at `/usr/local/lib` is left untouched. D-I1's version check is what protects a build from picking it up, and it is proved by configuring protoScala against `/usr/local` alone and getting a `FATAL_ERROR` where the same command used to succeed | agent; approved by the maintainer on 2026-09-23 |

### protoScala-specific consequences

| Decision | Taken by |
|---|---|
| protoScala's `find_package` floor is **2.1**, not the 2.0 the other four runtimes use, because the actor mailbox needs `ProtoMPSCQueue` (protoCore 2.1.0). The DEB and RPM relations carry the same floor | agent; approved by the maintainer on 2026-09-23 |
| **A0-8 is superseded.** The mailbox seam no longer selects its backend by grepping `protoCore.h` for the substring `newMPSCQueue`; it compares the protoCore *version* against 2.1.0 (plan risk R7). The probe read whichever header the discovery found first, so which mailbox implementation was compiled in depended on the discovery mode, and an installed 2.1 package and a sibling tree could disagree silently. With the 2.1 floor, 0.3.0 now ships the `ProtoMPSCQueue` mailbox, not the CAS'd-`ProtoList` fallback. The fallback code stays in `src/runtime/Mailbox.cpp` for a developer build against an older protoCore | agent; approved by the maintainer on 2026-09-23 |
| In developer-fallback mode the protoCore version is read from `../protoCore/CMakeLists.txt`'s `project()` call, and a version below 2.1.0 is a `FATAL_ERROR`. Without a package config there is nothing else authoritative to read | agent; approved by the maintainer on 2026-09-23 |
| `CPACK_DEBIAN_PACKAGE_SHLIBDEPS` is **not** enabled (the plan's Task 5 Step 4 proposed it; plan risk R9 materialised). `dpkg-shlibdeps` resolves every `NEEDED` entry to the distribution package that owns it, and no distribution owns `libprotoCore.so.2`: it fails with "cannot find library" and takes the whole `.deb` down. `libreadline` is therefore not declared, matching protoST and protoClojure | agent; approved by the maintainer on 2026-09-23 |

## Phase 3 — collections, prelude and string interpolation (2026-09-23)

Every entry is **[agent, pending review]**: taken by the implementing agent under
the maintainer's standing authorisation for the overnight run, and awaiting
review. The maintainer's standing rule was applied throughout: *follow Scala
where matching is cheap; where matching would cost real machinery and only
unusual code could notice, document the divergence with an id, a fixture showing
both answers, and a one-line cost justification.*

The plan's own Task 0 escalated nothing, so nothing below is a question. Sixteen
items were decided on cost by the plan itself (its "Decided on cost" table, C1–C16)
and are implemented as written; what follows is what the *implementation* decided,
which is what a review needs to see.

| Date | Decision | Taken by | Where |
|---|---|---|---|
| 2026-09-23 | **Task 1 Step 2 backed out after measuring it.** The plan asked for a SmallInteger fast path on the `EQ`/`NE` opcodes. Its premise is false: `valuesEqual`'s first branch already is that fast path (`Values.cpp:202` — two tag tests, then a word compare). Interleaved `perf stat -r 3` on this host showed no win on a loop dominated by `==` between integers (1.172 vs 1.180 Gcycles, inside the error bars) and an 8 % cycle regression on `sum_loop`, which never executes `EQ` (750 vs 693 Mcycles) — code layout in the hottest function of the runtime. The plan's own Step 5 says to back out above 3 %, so it is out, with the measurement recorded at the opcode | [agent, pending review] | src/runtime/ExecutionEngine.cpp |
| 2026-09-23 | The interning fix the plan asked for was **widened** from `unary_-`/`unary_!` to every binary operator symbol `slowBinary` sends (`+ - * < <= > >=`), because a class that overloads `+` re-interned its name on every application — the same defect, on a hotter path. `perf stat -r 3` on a 200000-iteration `unary_-` loop: 1.630 → 1.466 Gcycles, 10.0 % fewer. One `createSymbol` call is left in the file, on the terminal path of `throwMissingMember`, and says why at the call site; the plan's `grep -c createSymbol == 0` done-when is therefore not met literally | [agent, pending review] | src/runtime/ExecutionEngine.cpp, src/runtime/Runtime.cpp |
| 2026-09-23 | `O(args)` on an **object** now honours `apply`'s by-name parameters. `O.apply(args)` already did, so `Try { … }` would have evaluated its block on the caller while `Try.apply { … }` would not. DESIGN §5.1 makes the two the same call, so the sugar and the spelling it stands for must not disagree. This widens D53's reach rather than narrowing it, and is verified with an object of its own, not only through `Try` | [agent, pending review] | src/compiler/Compiler.cpp, tests/conformance/19-either-try/ |
| 2026-09-23 | The four `isXFast` predicates compare the **prototype word**, not the payload attribute. The plan's shape (`getAttribute(key) != nullptr`) is wrong on protoCore's contract: `getAttribute` answers `PROTO_NONE`, not `nullptr`, for an absent key, so every predicate answered `true` for every object kind and a `Range` was read as a `Vector` — a segfault on the first `(0 until 3) == List(0, 1, 2)`. Prototype identity is exact (none of the four is subclassable) and also tells a `Set` from a `Map`, which share the `__map__` payload key | [agent, pending review] | src/runtime/Values.cpp |
| 2026-09-23 | `sorted` checks the **orderable kinds first** instead of treating a throw from protoCore's `compare` as "not comparable". protoCore happily orders two unrelated object cells by address, so the plan's shape made `List(new Opaque(1), new Opaque(2)).sorted` answer a silently arbitrary order where D62 promises a loud error. Numbers (Char by code point), strings and booleans are ordered; anything else raises | [agent, pending review] | src/runtime/CollectionPrimitives.cpp, D62 |
| 2026-09-23 | `Map.size` counts **entries**, not slots. `ProtoMap::getSize` is the slot count, so two keys with a genuinely colliding hash counted as one; `keyedEqual` compared slot counts for the same reason | [agent, pending review] | src/runtime/CollectionPrimitives.cpp |
| 2026-09-23 | The List and Vector surfaces are **one set of natives installed on both prototypes**, and the shadowed `list_*` natives were deleted rather than left dead. A receiver-kind-driven rewrap makes `List.map` answer a `List` and `Vector.map` a `Vector` with no second algorithm. `vector-and-list-share-one-implementation.scala` applies 44 methods to both and reports the index of the first disagreement | [agent, pending review] | src/runtime/CollectionPrimitives.cpp, src/runtime/Primitives.cpp |
| 2026-09-23 | **D71 recorded**, a divergence the plan did not foresee and expected not to exist: a key that overrides `equals` but not `hashCode` misses at once here, while Scala's `Map1`..`Map4` compare by `==` alone and hide the classic defect up to four entries. At five entries and beyond scalac answers exactly what protoScala answers (verified: `size=6 lookup=miss`). Cost of matching: a second, unhashed small-map representation `ProtoMap` does not offer, to preserve a behaviour Scala itself loses at five entries and that only the classic `equals`/`hashCode` bug can observe. The fixture carries both answers | [agent, pending review] | docs/STATUS.md D71, tests/conformance/18-maps-and-sets/map-equals-without-hashcode-misses.scala |
| 2026-09-23 | `Range.reverse` answers a **`Range`** and an empty Range renders as `empty Range 5 until 5`, because both are what scalac answers and both cost four words. Matching where matching is cheap, as the standing rule says | [agent, pending review] | src/runtime/CollectionPrimitives.cpp |
| 2026-09-23 | In an `f` literal, `%%` is a literal percent and a **bare `%` is a compile error**, with scalac's own rule ("conversions must follow a splice; use %% for literal %"). Verified: scalac rejects `f"50% of $x"` too. The plan did not specify it, and passing the literal through unchanged printed `42%%` | [agent, pending review] | src/compiler/Compiler.cpp, tests/conformance/16-strings/interpolation-f-bare-percent.scala |
| 2026-09-23 | The lexer's `$name` scan stops at `$`. `$` is a legal identifier character in Scala, so `s"$a$b"` lexed as one hole named `a$b`; scalac reads it as two holes, which is what the parser now does, while `_` stays part of the name | [agent, pending review] | src/frontend/Lexer.cpp |
| 2026-09-23 | The capture pre-pass descends into an interpolation's holes. A name read inside a hole was never boxed, so `var v = 1; val f = () => s"$v"` would have read a stale value; `releaseChildren` and `rebase` now walk the tree through one table so a node kind that grows a child cannot be remembered by one walker and forgotten by the other | [agent, pending review] | src/frontend/Compiler.cpp, src/frontend/AST.cpp |
| 2026-09-23 | The 22 pre-existing `-Wmissing-field-initializers` warnings the by-name work left on `main` were fixed, so the tree builds warning-free as the standard requires. Six files, one `{}` each; no behaviour change | [agent, pending review] | src/compiler/ |
| 2026-09-23 | `docs/DESIGN.md` §6.1 was already edited in the working tree when the run began, carrying the C1 and C2 rulings. The plan's Preflight says to stop on an unclean tree; the edit is exactly what Task 14 Step 5 expects to find, so the run continued and verified it (`grep -c 'maintainer ruling, 2026-09-23'` = 2) rather than stopping | [agent, pending review] | docs/DESIGN.md §6.1 |
| 2026-09-23 | Three plan expectations were **arithmetically wrong** and the fixtures carry the measured values instead: `negate-boundary` (`-(min + 1)` is `+9007199254740991`, confirmed by scalac), `interpolation-f-basic` (`pct=42%`, not `50%`), and `list_ops`'s fold (`9999900000`, not `333283335000`) | [agent, pending review] | tests/conformance/, benchmarks/comparable/list_ops.scala |
| 2026-09-23 | The low-heap sweep is green **except** `Mailbox.EightProducersLoseNothingAndDuplicateNothing`, which aborts under `PROTOCORE_HEAP_LIMIT_CELLS=20000`. Verified pre-existing: it fails the same way on `main` at `bca0352` with this phase's changes stashed. Not diagnosed here — it is Phase 5 code and outside this phase's scope — but recorded rather than left to be rediscovered | [agent, pending review] | tests/unit/test_mailbox.cpp |

## Still open for the maintainer (2026-09-23)

These two are open **questions**, not agent decisions, and the review of
2026-09-23 did not settle them. They are recorded here so they are not mistaken
for approvals.

| Question | Why it is open |
|---|---|
| Does protoCore's `[Unreleased]` CHANGELOG section fold into the **2.1.0** entry, or become **2.1.1**? | The Phase I packaging work wrote an `[Unreleased]` entry and deliberately left `project(protoCore VERSION 2.0.0 …)` alone, while the P2 plan bumps 2.0.0 → 2.1.0. Whether the packaging change rides along with 2.1.0 or gets its own patch release is the maintainer's call; it decides what `find_package` floors and package relations the family declares |
| Does the **`saturation-8` / `saturation-32` addition to DESIGN §8.5** stay? | Those two benchmark modes deviate from the section's own 1,000,000-trivial-message rule: they put the cost inside the handler to separate scheduler limits from actor-count limits, and they are the only modes that read the physical core count. They were added to mirror protoST's `saturation_8a.st` / `saturation_32a.st`. Keeping them means DESIGN §8.5 no longer has one uniform rule |

---

# Phase 4 — exceptions, `super[T]`, enums, named arguments, extensions (2026-09-24)

Taken by the implementing agent under the maintainer's standing authorisation for
the unattended run, and awaiting review. The plan
(`docs/plans/2026-09-23-phase-4-exceptions-enums.md`) escalated four items
(E1–E4) to the maintainer; the maintainer was unavailable, so each was decided
here, implemented, and recorded below with the argument and what reversing it
would cost.

**Deviation-id range.** The plan reserved D71–D89 on the assumption that Phase 3
had taken D54–D70. Phase 3 in fact reached **D71** (D57, D60 and D64 allocated but
deliberately unused), so the whole block was shifted one place: Phase 4 uses
**D72 onwards**, keeping the plan's relative order. Every plan reference to
`D71`..`D85` therefore reads one higher in the tree.

## The four escalations

| # | Decision taken | Argument | Proved how | Cost to reverse |
|---|---|---|---|---|
| **E1** | **Where the in-flight exception value lives while the stack unwinds: option (c).** Every frame the exception passes through re-roots it in its own `ProtoContext::returnValue`, unconditionally and before the handler search | protoCore's root scan reads `currentCtx->returnValue` for every context on the thread's context stack (`protoCore/core/ProtoSpace.cpp:352-356`), and `~ProtoContext` anchors a non-null `returnValue` into the **parent's** young chain before submitting its own young generation (`ProtoContext.cpp:239-258`). Writing the payload there therefore makes it a root in every frame it passes through, rather than leaving it to protoCore's generational timing | **Not proved by breaking the premise, and this entry says so rather than claiming otherwise.** `20-exceptions/catch-payload-survives-unwinding.scala` and `catch-payload-survives-a-concurrent-collector.scala` exercise the path (400 and 200 throws through 60- and 40-frame chains, under collector pressure, with a second thread allocating) and are green — but deleting the re-rooting line leaves them green too. Two things mask it: the handler entry writes the payload into a traced automatic local *before* anything on the handler path allocates, and a freshly submitted young generation is not a candidate of the same collection cycle. The window is not observable from the language, and an attempt to force a collection inside it by calling `triggerGC()` from the C++ catch crashed with and without the re-rooting, so it proved nothing. The line is kept as the guarantee that the payload's reachability does not depend on either masking effect | Removing it is one line and, today, unobservable. Adding a per-thread pin stack instead is additive and costs one attribute write per throw |
| **E2** | **The handler search sits OUTSIDE the C++ catch block**, as a retry loop around `runLoop` (`ExecutionEngine::runFrame`): the handler body is entered by `continue`, so it runs with no live C++ handler | The plan's stated reason — that entering the handler inside the catch breaks cooperative `await` — **did not hold when tested**, and this entry corrects it: a `FutureYield` thrown from a handler body entered inside the catch propagates out perfectly well, the snapshot's `ip` points into the handler body, and the resume re-enters it through `runFrame` with nothing in flight. The real reason the retry loop is required is stronger and simpler: **a frame must be able to catch a second exception** — one raised by its own handler body, by a non-matching cascade's `RETHROW`, or by a `finally` — and only a loop that re-enters the frame's own `try` region can. Entering the handler from inside the catch abandons the retry loop, so the frame's handler table is no longer consulted | **Proved by breaking the premise.** Replacing the `continue` with a recursive `runLoop` call from inside the catch turned **11 fixtures red**, including `nested-try`, `nested-try-braces`, `catch-in-a-loop`, `catch-in-a-loop-braces`, `finally-in-a-loop`, `finally-runs-on-failure`, `finally-that-throws-replaces` and `rethrow-from-a-handler` — each of them an exception the frame could no longer catch. It also grows the native stack once per caught exception, which `finally-in-a-loop`'s 100000 cycles would overflow | Doing it inside is a smaller diff and a loud, immediate defect rather than a silent one — the 11 fixtures are the guard |
| **E3** | **A0-7's six invariants are adopted as written**, with `resumeFrames` calling `runFrame`, no `finally` on a suspension, and a failed `await` raising at its call site (retiring **D50**) | Each invariant is one code path, and each now has a fixture whose failure is visible | **Proved by breaking the premise, invariant by invariant.** Invariant 1: `ScalaThrowShape.AFutureYieldIsNotAStdException`. Invariant 2 — making `runFrame` run the Finally handler on a `FutureYield` turned `finally-does-not-run-on-suspension.scala` red (`cleanup cleanup body=1` instead of `cleanup body=1`) and `await-inside-finally.scala` red (an uncaught sentinel). That fixture was **rewritten** when the first version proved toothless: it counted cleanups in a local variable, and the frame snapshot is taken before the retry loop sees the suspension, so a wrongly-run cleanup's increment was discarded and the fixture passed with the bug present. It now prints from the cleanup instead. Invariant 3 — pointing `resumeFrames` back at `runLoop` turned `await-inside-finally.scala` red (the resumed frame had no handler table, so the `RETHROW` after the cleanup escaped and the outer `catch` never saw it). Invariant 4 is covered by `await-inside-catch.scala`, which reads `e.getMessage` after a suspension inside the handler; the saving mechanism is Phase 5's and was not broken on purpose. Invariant 5 — restoring the old abandon-the-chain behaviour turned `await-a-failed-future-inside-try.scala` and `await-a-failed-future-runs-the-finally.scala` red. Invariant 6 — removing `deliver`'s `catch (ScalaThrow&)` made `actor-survives-a-handler-exception.scala` produce no output and `await-a-failed-future-inside-try.scala` time out | Each invariant is one code path; the named fixtures are what would catch a regression |
| **E4** | **Extension methods are global and session-wide (D82), with no import scoping**, and an extension whose name collides with an existing member of the receiver type is a **compile error** (D83) | This is a public-surface and scheduling question, not a protocol one. Scoping an extension needs an import mechanism, and protoScala has none: `import` is parsed and ignored, and the mechanism arrives with UMD in Phase 6 (ROADMAP). Shipping unscoped extensions now is what makes the feature usable at all, and it is what closes Phase 3's custom-interpolator restriction. The collision error is what keeps the unscoped form from being unrecoverable: silently shadowing `String.length` for a whole session would be a defect nothing could undo | Argument plus the compile-time collision check; no protocol is involved, so no breaking test is owed | Adding import scoping later is additive for code that does not rely on the global visibility, and Phase 6's done-when now records it |

## Decisions the implementation took, beyond the plan

| Date | Decision | Taken by | Where |
|---|---|---|---|
| 2026-09-24 | **The deviation-id block was shifted by one** (D72 onwards, not D71), because Phase 3 reached D71 rather than D70 as the plan assumed | [agent, pending review] | docs/STATUS.md, docs/LANGUAGE.md |
| 2026-09-24 | **No `__throwableClasses` list in the prelude.** The plan asked for a `List` of `[name, factory-lambda]` pairs for `materialiseError` to call. The exception classes are resolved by their **type key** instead (`GlobalTable::findType(name)->key`, read out of the globals object exactly as `SEND_SUPER` reads an owner), and `materialiseError` uses `ExecutionEngine::construct`. That removes twenty lambdas and twenty two-element lists from every session's start-up, needs no new prelude surface, and keeps the REPL-shadowing rule (`@Some#1`, D25) for free | [agent, pending review] | lib/prelude.scala, src/runtime/ActorPrimitives.cpp, src/runtime/ExecutionEngine.cpp |
| 2026-09-24 | `Throwable.getClass` is implemented over a new internal global **`__classNameOf`**, not the plan's `productPrefixOf`. Every runtime-provided global that is not part of the Scala surface carries the `__` prefix in this tree (`__raise`, `__tryOf`, `__fmt`), and `productPrefixOf` would have added a public non-Scala name to every session's namespace | [agent, pending review] | src/runtime/Primitives.cpp, lib/prelude.scala |
| 2026-09-24 | `UninitializedFieldError` extends **`RuntimeException`**, not `Error` as the plan's table says, because `scala.UninitializedFieldError` extends `RuntimeException` despite its name and matching that is free | [agent, pending review] | lib/prelude.scala |
| 2026-09-24 | **D87 recorded**, an interaction the plan did not foresee. The cleanup a `return` inlines before its `RETURN` is **excluded from its own `try`'s handler ranges** — the table holds two non-overlapping `Finally` pieces instead of one — because the `try` has already been left and re-entering its own handler would `RETHROW` a slot it never wrote, which would surface as `protoscala: internal error`. That fixes the reachable case (`finally-throws-on-return.scala`, verified against scalac). What is *not* implemented is the exotic multi-level variant: a cleanup inlined at a `return` is still covered by the ranges of the `try` constructs **between** the return and the cleanup's owner, so `try { try { return 1 } catch { … } } finally { throw x }`-shaped programs may let an inner handler see an exception raised by an outer cleanup. Cost of matching exactly: a per-construct hole stack keyed by nesting depth, for an interaction no reasonable program reaches | [agent, pending review] | src/compiler/CompilePatterns.cpp, docs/STATUS.md D87 |
| 2026-09-24 | A one-line `catch case e: T => …` (the first `case` on the `catch` line, with no bracket of its own) parses its case bodies **directly** rather than through `parseCaseClause`. That clause parser needs a terminator token to stop at and there is none here: the body ends at the newline or at the enclosing region's `Outdent`. One expression per body; a multi-statement body uses braces or an indented block after `=>`, both of which the ordinary expression parser already handles | [agent, pending review] | src/frontend/Parser.cpp |
| 2026-09-24 | `Parser.LaterPhaseConstructsAreReportedClearly` was **rewritten, not deleted**: it asserted that `throw`, `try … finally` and `super[T].f` report "not implemented yet", which this phase implements. It now keeps only `new A { }` (still out, D80) and a new `Parser.ThrowAndTryParse` pins the trees the new syntax produces | [agent, pending review] | tests/unit/test_parser.cpp |
| 2026-09-24 | Three fixtures were updated because **the payload type changed by design** (D44 retired), not to make a red test pass: `14-futures/try-matches-scalac.scala` (`Failure(RuntimeError(…))` → `Failure(new RuntimeException(…))`, which is now byte-for-byte what scalac prints, so the "only difference is the error value" note was deleted), `13-actors/await-refused-inside-map.scala` and `tutorial/13-actors-failure.scala` (`e.className` → `e.isInstanceOf[…]` / `e.getClass`) | [agent, pending review] | tests/conformance/ |
| 2026-09-24 | `ProtoSparseListIterator::advance` is **non-const**, so the plan's iteration code does not compile as written; the iterator is advanced through a `const_cast`, which is protoCore's own idiom for it. The plan's Task 11 snippet has the same defect | [agent, pending review] | src/runtime/ExecutionEngine.cpp |
| 2026-09-24 | Two fixtures needed a **non-tail-recursive** `deep` (`1 + deep(n + 1)`): scalac turns a self tail call into a loop, so the reference program never overflows and the comparison ran forever | [agent, pending review] | tests/conformance/20-exceptions/native-stack-overflow-is-catchable.scala, error-is-not-an-exception.scala |
| 2026-09-24 | `throw-a-non-throwable.scala` reaches the `THROW` check through `asInstanceOf[Any]`, not `asInstanceOf[RuntimeException]`: a cast to a named class is checked and fails earlier, with a `ClassCastException`. The fixture says so | [agent, pending review] | tests/conformance/20-exceptions/throw-a-non-throwable.scala |
| 2026-09-24 | **`enum` needs no native method at all**, which the plan did not foresee: `ordinal` is an ordinary `val` the desugarer writes on each case, and `toString` is Product's, which already answers the case's simple name. `Enum.ordinal` is declared **abstract** so a case implements it without `override`. The plan's `prim_enum_ordinal`/`prim_enum_toString` and the `__ordinal__`/`__enumname__` layout keys were therefore not added | [agent, pending review] | src/frontend/Desugar.cpp, src/compiler/ClassInfo.cpp |
| 2026-09-24 | **The cases are generated INSIDE the companion**, not beside it, so Task 7's nested-template lifting gives them the qualified names `Colour.Red` Scala requires. `values`, `valueOf` and `fromOrdinal` are generated as **source text and parsed**, so they cannot drift from what a user could write; their messages are scalac's own, verified against it | [agent, pending review] | src/frontend/Desugar.cpp |
| 2026-09-24 | **`values` and `valueOf` are generated only when every case is a singleton**, and `fromOrdinal` always, covering the singletons alone — measured against scalac, which reports "value values is not a member of object Tree … it has non-singleton cases". The plan did not state this | [agent, pending review] | src/frontend/Desugar.cpp, tests/unit/test_desugar.cpp |
| 2026-09-24 | **D78 was not taken.** The plan proposed accepting an `enum` case without its qualifier (`Red` as well as `Colour.Red`). Scala requires the qualified form unless the case is imported, matching it costs nothing, and a deviation that accepts more is still a deviation — so the qualifier is required and the id is left unused | [agent, pending review] | tests/conformance/21-enums/enum-case-needs-the-qualifier.scala, docs/STATUS.md D78 |
| 2026-09-24 | **`TemplateDef::enclosing` was not added.** The plan asked for a field recording the enclosing objects' dotted path; the lifted template's own **name** already is that path, so a second copy would be a field that can disagree with it | [agent, pending review] | src/frontend/Desugar.cpp |
| 2026-09-24 | A lifted template keeps the **qualified name for resolution** and shows the **simple** one: `object O { case class C }` prints `C(1)`, which is what scalac prints. Without the split it printed `O.C(1)` | [agent, pending review] | src/compiler/CompileTemplates.cpp, src/compiler/ClassInfo.h |
| 2026-09-24 | A sibling named in a lifted template's `extends` clause is **qualified at lift time**, because a parent is resolved in `buildClassInfo` before any template scope exists. Unqualified sibling references in *expressions*, *types* and *extractor patterns* are resolved instead by trying the enclosing prefixes innermost-first, which is Scala's scoping | [agent, pending review] | src/frontend/Desugar.cpp, src/compiler/Compiler.cpp, src/compiler/CompilePatterns.cpp |
| 2026-09-24 | **`NEW` carries a KwSendSite for `new C(x = 1)`** instead of the plan's `PUSH_GLOBAL <@C>; args; SEND_KW <init>`, which would have passed the CLASS as `this` to the constructor rather than a fresh instance. No extra opcode was needed | [agent, pending review] | src/compiler/CompileTemplates.cpp, src/runtime/ExecutionEngine.cpp |
| 2026-09-24 | **A default block mirrors the callee's whole slot prefix** — its parameters, its locals and its captures, at their own slot numbers — and its own arity carries that count to the run time. The plan's shape (arity = the number of parameters before the default) cannot reach a capture, so `def f(a: Int)(b: Int = a + 1)`, which scalac accepts, did not compile. Two further fixes were needed and each removed a SILENT inconsistency: the capture analysis now walks the defaults, so an enclosing local a default names is boxed as one the body names would be; and a discarded pre-pass compiles each default with the callee as its enclosing function, forcing the callee's captures through the ORDINARY resolver. Without them a default read a hoisted, not-yet-assigned slot — but only when the body happened not to mention the same local | [agent, pending review] | src/compiler/Compiler.cpp, src/runtime/ExecutionEngine.cpp, tests/conformance/23-named-arguments/ |
| 2026-09-24 | **A default value is refused on a method that also takes a repeated parameter**, because the callee cannot tell an omitted default from an empty repeated argument. Scala allows the combination; guessing would be silent | [agent, pending review] | src/compiler/Compiler.cpp |
| 2026-09-24 | **D88 and D89 recorded**, neither foreseen by the plan and both found by comparing with scalac: a default may read a parameter of the same list, which scalac rejects (it requires a previous list); and a named argument on a function value binds against the literal's own parameter names, which scalac rejects because `Function2.apply` names its parameters `v1` and `v2`. Both accept strictly more programs than Scala, and each carries a fixture with both answers | [agent, pending review] | docs/STATUS.md D88, D89 |
| 2026-09-24 | **Class prototypes are MUTABLE** (`MAKE_CLASS` builds `newChild(ctx, true)`). This is a change to the object model, which is normally a maintainer decision; the plan anticipated it ("if a prototype turns out to be immutable, the installation needs a dedicated native, and that is a Task 0 addendum, not a silent workaround") and it is taken here with the measurement the rule demands. It is **required**, not an optimisation: an extension is installed after the class exists and every instance already created must see it, and an immutable prototype's `setAttribute` answers a NEW object that existing instances' chains do not point at. It also makes `superSend`'s pointer-identity search robust against any later write. Cost, `perf stat -r 3`: `object_tree` +2.8 % cycles / +1.4 % instructions, `attr_lookup` inside its ±6 % error bars — under the phase's 3 % gate. Class creation is cheaper in exchange. Reversing it means giving each class a dedicated mutable holder in its chain (one extra object and one chain hop per class) | [agent, pending review] | src/runtime/ExecutionEngine.cpp, benchmarks/RESULTS.md |
| 2026-09-24 | **`__installExtension`, one new internal global**, rather than `PUSH_GLOBAL <@T>; fn; SET_FIELD m`. A primitive prototype (`intProto`, `stringProto`, `listProto`, `unitProto`) has no global of its own to push, and the native also carries the run-time half of the collision check, because the compiler does not describe a primitive prototype's members. It marks each installed member `$ext$<name>`, so re-running the same definition replaces it while a genuine member of the type stays protected — a reloaded script and a repeated REPL line both work | [agent, pending review] | src/runtime/Primitives.cpp, src/compiler/Compiler.cpp |
| 2026-09-24 | **One targeted Layout rule for the collective-extension header.** The closing bracket of `extension (n: Int)` opens an indented region, exactly as the `)` of an old-style condition does, because that indented form is the **only** one scalac accepts — it rejects `extension (n: Int):` with "no `:` expected here" (verified). protoScala therefore rejects the colon form too, with a message saying what to write instead: accepting it would have been a deviation for no gain | [agent, pending review] | src/frontend/Layout.cpp, src/frontend/Parser.cpp |
| 2026-09-24 | **`StringContext` is a `final case class`** taking `parts: String*`, so `StringContext("a", "b")` works as a term without a hand-written companion, and the desugarer lowers an interpolation through `new StringContext(...)` so the lowering does not depend on a companion existing at all | [agent, pending review] | lib/prelude.scala, src/frontend/Desugar.cpp |
| 2026-09-24 | **The interning test found the trap's sharp edge**, which the plan did not state: protoCore embeds a SHORT string in the pointer word, so `fromUTF8("dtype") == createSymbol("dtype")` and a non-interned key works **by accident** for a short parameter name. The bug appears only for a name too long to embed, which is exactly why it is hard to find. Both halves are asserted, and a conformance fixture exercises a long name end to end | [agent, pending review] | tests/unit/test_exceptions.cpp, tests/conformance/23-named-arguments/named-argument-with-a-long-name-binds.scala |
| 2026-09-24 | **A native that ignores `keywordParameters` reports an arity mismatch** rather than a dedicated "does not take named arguments" message: the engine cannot tell whether a native reads the keyword list. The call still FAILS loudly and names the method, which is the bar; scalac rejects it at compile time, and D81 already covers the class of divergence | [agent, pending review] | src/runtime/ExecutionEngine.cpp, tests/unit/test_engine.cpp |
| 2026-09-24 | Eight unit tests that asserted a Phase 4 construct was **unimplemented** were rewritten, not deleted: `Parser.LaterPhaseConstructsAreReportedClearly` (now only `new A { }`), `ParserDefs.MoreUnsupportedAndInvalidDefinitions`, `ParserDefs.UnsupportedDefinitionsAreReportedClearly`, `ParserTemplates.Errors`, `Compiler.SemanticErrors` and `CompilerInterpolation.AnUnknownInterpolatorIsRejectedWhileDesugaring`. Each now pins the new behaviour or a neighbouring construct that is still out | [agent, pending review] | tests/unit/ |
| 2026-09-24 | **The actor benchmark suite was re-run and its first attempt was discarded**, because the clean rebuild of Task 14 Step 1 deleted `build_release` under it and it died with `FileNotFoundError` after writing no report. Recorded rather than quietly re-run: a harness that reports a missing binary as an exit-0 traceback is worth knowing about | [agent, pending review] | benchmarks/run_actor_benchmarks.py |
| 2026-09-24 | The unknown-native-class fallback is reached through **`__raise`**, the internal primitive that raises an arbitrary class name, because every class name the runtime actually raises is defined in the prelude and the fallback is otherwise unreachable from source. No `#ifdef PROTOSCALA_TESTING` natives were added | [agent, pending review] | tests/conformance/20-exceptions/unknown-native-class-falls-back.scala, tests/unit/test_exceptions.cpp |
| 2026-09-24 | **The < 25 ms cold-start budget is accepted as missed for 0.5.0** rather than shrinking the prelude or precompiling it inside this phase, and the miss is attributed by **measurement, not assertion**. 0.4.0 (built from `941f577` in a scratch worktree against the same protoCore) and 0.5.0 were re-measured interleaved in one window, three rounds of 21 verified runs, alongside a third, unshipped probe binary carrying **twenty more exception classes of the same shape** in its prelude: 23.91 -> 25.22 -> 26.41 ms, monotone in every round. Phase 4 cost +1.31 ms and the twenty probe classes a further +1.19 ms, so at roughly 60 us per prelude class the prelude's growth from 156 to 200 lines accounts for the whole regression -- which **rules out** the phase's engine changes as the cause (the retry loop measured free, mutable prototypes make class creation cheaper). The alternative was to cut prelude classes, and the exception hierarchy is the one thing this phase exists to provide. Reversing the cost means a precompiled prelude, which belongs with UMD's module loading in Phase 6 and is listed there | [agent, pending review] | benchmarks/RESULTS.md, README.md, CHANGELOG.md, docs/ROADMAP.md |

---

# Phase 6 — modules, UMD, the precompiled prelude and packaging (2026-09-24)

Taken by the implementing agent under the maintainer's standing authorisation for
the unattended run, and awaiting review. The plan
(`docs/plans/2026-09-24-phase-6-umd-packaging.md`) escalated three items (E5-E7)
to the maintainer; the maintainer was unavailable, so each was decided here,
implemented, and recorded below with the argument and what reversing it would
cost.

**Deviation-id range.** Re-checked against `main` before a single id was written:
`grep -oE 'D[0-9]+' docs/STATUS.md | sort -n | tail -1` reported **89**, exactly
as the plan predicted, so Phase 6 uses **D90-D96** with no shift. D57, D60, D64
and D78 remain deliberately unused and were not recycled. **No opcode was
allocated**; the lowest free one is still 40 and STATUS now says so under its
opcode table, because a reader who has just read a UMD changelog will look for a
module opcode.

## The three escalations

| # | Decision taken | Argument | Proved how | Cost to reverse |
|---|---|---|---|---|
| **E5** | **The cold-start budget: the image was built, the split was measured first, and the result is reported as measured rather than claimed.** See "The cold-start verdict" below for the numbers, which are the decision's substance | The plan fixed the decision rule *before* the measurement existed, so the branch could not be rationalised afterwards: let `R = run / total`; `R < 0.55` means an image is worth building. Measured `R = 177 / 4,426 = 0.040`, so the image removes 84.8 % of the prelude's cost and the branch is unambiguous. What an image cannot remove is `linkSymbols` (342 us) and *running* the compiled prelude (177 us), and protoScala has no way to remove those: protoCore has **no object-graph serialisation, no image and no snapshot API** — verified across `headers/protoCore.h` and `proto_internal.h`, where the only caching facility is the in-process `SharedModuleCache`, which holds live objects and cannot cross a process | 21 verified runs per figure, medians, with the load average recorded beside each; the split reproduced to within 0.5 % across two separate batches taken at load 11.2 and load 5.2. Both prelude paths live in one binary (`PROTOSCALA_PRELUDE_NO_IMAGE=1`), so the third column is what proves the *image*, and not the phase, moved the number | Removing the remaining cost needs a **protoCore space image** — a new protoCore capability with a GC story of its own, which P3 makes a maintainer decision — or a change to DESIGN §1's number, which is one line in DESIGN and one in the README and is reversible. **The plan brings the measurement rather than a guess; which of the two gives is the maintainer's call** |
| **E6** | **Ruling B: extensions stay global and session-wide. D82 stands, restated to say that scoping was considered and DECLINED** | Three reasons, in order of weight. (1) **It would be a third behaviour, not convergence.** protoScala hoists an import to its compilation unit (D96), so "scoped" here means per-*unit*, which is neither Scala's per-block lexical scoping nor today's session-wide visibility. Shipping that as "extensions are now scoped like Scala's" would be a worse divergence than the honest one, because it *looks* like convergence. (2) **It is entangled with D96 and should be decided with it, not before it.** Lexical scoping needs a scope-aware name resolver the compiler does not have; once that exists, imports and extensions are scoped together, and extensions scoped alone now would have to be redone. (3) **It is a public-surface change that breaks working programs** — an extension defined in a module and used without importing it stops working — for a benefit that is partial until (2) lands | Three fixtures pin what the behaviour actually is, so a later phase flips a test rather than rediscovering a design: importing the module makes the extension visible; an extension **leaks** into another module that imports nothing (`util/UsesSquared.scala` writes `n.squared` and works only because something else in the session loaded `util/Extras`); and with nothing loading it the send fails at run time — late detection, which the platform accepts, and not silent failure, which it does not | Ruling A is additive for code that does not rely on global visibility, and the plan's Task 9 Step 2 specifies it in full. It narrows D83 (a collision would become a collision *within one unit's visible set*) and it makes extensions per-unit rather than per-block, so it is a second divergence rather than a convergence. The fixture that would turn red is named above |
| **E7** | **The plan's recommendation, taken: both Python fixtures stay `XFAIL`, their directive is rewritten to name the real blocker, and ROADMAP gains Track Y.** Neither was converted, no stand-in was registered under `py`, and no expectation was weakened | ROADMAP's Phase 4 hand-off says "what is missing is only `import py.…` routing". The routing shipped in this phase and the fixtures still cannot be converted, because three things are missing that protoScala cannot supply: no runtime in the family registers the alias `py`; protoPython's providers are installed by a whole `PythonEnvironment`, so having them here means a second runtime in the process, which is **R5**; and `protoscala` links no sibling runtime. So ROADMAP contradicts ROADMAP's own done-when (which asks only for a stand-in provider and protoST's when both are built), INTEROP §3 and DESIGN §11's R5 — and a contradiction between documents is escalation territory. A wrong reason on a fixture is worse than no reason, so only the reason changed | All three facts re-verified on the day: `grep registerProvider` over protoPython gives `native`, `python_stdlib`, `compiled`, `hpy`; `ldd build_release/protoscala` lists `libprotoCore.so.2` and `libreadline.so.8` and nothing else of the family; and a unit test asserts that `getProviderForSpec` returns `nullptr` for all four family aliases in a stock process. The **keyword convention** is now exercised for real instead — `25-interop/stand-in-provider-keyword.scala` sends a named argument through a provider loaded by `dlopen` and reads it back out of `keywordParameters`, with a sibling whose parameter name is too long to embed in a pointer word | Ruling "convert them" cannot be implemented without a `py` provider. Ruling "amend the done-when" is one ROADMAP edit and one new track, which is what was done — **marked pending review, because amending a done-when is the maintainer's**. What must not happen, and did not: a stand-in registered under `py` answering `float64` would be a green test asserting numpy behaviour against a stub |

## The cold-start verdict (E5's substance)

Recorded in full in `benchmarks/RESULTS.md`. The short form:

- **Where the prelude's time went, before anything was built** (21 runs, medians):
  parse 2,640 us (59.6 %), desugar 115 us, compile 997 us (22.5 %), link 342 us,
  run 177 us (4.0 %), total **4,426 us**. `lib/prelude.scala` is 200 lines /
  10,176 bytes with 30 top-level declarations and 103 `def`s; 4,426 us over 30
  declarations is ~148 us each for the whole pipeline, against the *marginal*
  60 us/class the 0.5.0 attribution measured by adding twenty classes. A marginal
  cost below the average is what a parser with fixed set-up work produces, so the
  two measurements agree.
- **What the image removed**: parse and desugar go to zero and the reconstruction
  replaces compile. Prelude total **4,116 us -> 1,660 us**, a saving of
  **2,456 us**, with link and run unchanged as predicted.
- **The budget itself** is DESIGN §1's `< 25 ms`, and it is a done-when rather
  than an aspiration: `benchmarks/cold-start.sh <binary> 21` must exit 0 with
  `verified=21`. **It is met.** Three rounds interleaved in one window, image and
  source path alternating, load average 2.97 at the start and 2.67 at the end,
  and **all twelve cases reported `verified=21`**:

  | binary | script | REPL |
  |---|---:|---:|
  | 0.5.0 (quoted, not re-measured) | 25.22 ms | 25.58 ms |
  | **0.6.0, precompiled image** | **23.73 ms** | **23.89 ms** |
  | 0.6.0, `PROTOSCALA_PRELUDE_NO_IMAGE=1` | 25.65 ms | 26.01 ms |

  `cold-start.sh` exited **0 in all three image rounds** and **1 in all three
  source rounds**, on both cases. The third row is the decisive one: both paths
  live in one binary, so it is the **image** that moved the number and not
  anything else in the release, and the source path still misses by 0.65 ms.

  The saving, 1.92 ms on the script case, agrees with the 2.46 ms the in-process
  stage measurement predicted, to within the run-to-run spread. **Nothing was
  claimed on an unverified run, and the budget would have been reported as missed
  if it were** — the wording for that outcome was written before the measurement
  existed, exactly as 0.5.0 honestly reported its own miss.

  **E5 therefore no longer needs a ruling to ship**, and the question it raised
  stays open on its merits rather than as a blocker: a protoCore space image is
  still the only way to remove the remaining 519 µs (`linkSymbols` 342 µs plus
  running the prelude 177 µs), and the budget is met with about 1.3 ms of
  headroom — which is what the next twenty prelude classes would spend.

## Decisions the implementation took, beyond the plan

| Date | Decision | Taken by | Where |
|---|---|---|---|
| 2026-09-24 | **The generated prelude image holds tables ONLY; the reconstruction that walks them is a hand-written source file.** The plan proposed copying a fixed `buildPreludeImage` verbatim into the generator's output "so it cannot drift per prelude". A file that is never generated serves that intent more strongly and stays diffable and reviewable in the repository, so `src/runtime/PreludeImage.cpp` is ordinary code and the generator emits `preludeImageData()` alone | [agent, pending review] | src/support/PreludeImage.h, src/runtime/PreludeImage.cpp, src/tools/PrecompileMain.cpp |
| 2026-09-24 | **`GlobalTable::bind` and `aliasType` seed the key counters.** Neither goes through `declare`, so without this a name installed from the image or from an import left its counter unset, and a REPL redefinition of a prelude name would have been handed the prelude's own key and silently overwritten it. Found by a test, not by reasoning | [agent, pending review] | src/compiler/GlobalTable.h, tests/unit/test_prelude_image.cpp |
| 2026-09-24 | **The image also replays the by-name selector index and the whole type namespace.** The plan's record list covered bindings and `ClassInfo`s. `CaptureAnalysis`'s conservative union decides boxing for call sites the compiler cannot resolve, and dropping it would have UNDER-boxed a call to a prelude `def` with a by-name parameter — a missing Cell is not harmless where an extra one is. The type namespace is emitted as its own `(name, key)` table because a `ClassInfo`'s `name` and its namespace entry are different things (`object Try`'s class is named `Try`, its entry is `Try.type`), and collapsing them renamed every companion object's class | [agent, pending review] | src/support/PreludeImage.h, src/tools/PrecompileMain.cpp |
| 2026-09-24 | **`builtinByNameSignatures()` moved to `support/BuiltinNames` alongside `builtinGlobalNames()`.** The plan moved only the names. The build-time tool must apply the same by-name signatures before compiling the prelude, or a prelude call to a builtin with a by-name parameter compiles differently in the tool than in the runtime — an image that is silently wrong | [agent, pending review] | src/support/BuiltinNames.{h,cpp}, src/runtime/Primitives.h |
| 2026-09-24 | **An `import` and an `extension` in a module stay OUTSIDE the synthetic object.** `desugarModule` as the plan wrote it moved every top-level statement inside, where a template body **skips** `Import` nodes — so a module that imported another module had its import silently ignored, and an extension was refused outright ("an extension must be defined at the top level of a file"). Neither is a member of anything, so both are hoisted. Consequence, recorded in D91: an extension in a module sees the module's globals and not its members | [agent, pending review] | src/frontend/Desugar.cpp, docs/STATUS.md D91 |
| 2026-09-24 | **A template-level `import` is compiled rather than skipped.** `CompileTemplates.cpp` skipped `Import` nodes, which was correct while `import` was parsed-and-ignored and is a silent trap now | [agent, pending review] | src/compiler/CompileTemplates.cpp |
| 2026-09-24 | **`KwGiven` added to the layout pass's `canEndStatement`.** `import M.given` is the only statement that can end with the bare keyword (a `given` *definition* is refused by D3, and a `using`/`given` parameter list is inside brackets); without it the next line was read as a continuation of the import | [agent, pending review] | src/frontend/Layout.cpp |
| 2026-09-24 | **Forcing a module's singleton installs the thread's active call context.** A module's top level can call a native (`println` is the obvious one), and it is forced from the COMPILER, not from `run()`, so nothing had installed the context and the native read a null layout — a segfault on `import effects.Setup` | [agent, pending review] | src/repl/Session.cpp |
| 2026-09-24 | **An imported member alias is compiled through a hidden qualifier `__module$M`** rather than a new resolution rule: the module object is bound under a name no user can write (`$` cannot occur in a protoScala identifier) and every imported member compiles as an ordinary `Select` on it, so the emitted code is byte-for-byte what the qualified spelling emits and no opcode was needed. `import util.Strings.{shout}` still binds `shout` and **not** `Strings`, as in Scala | [agent, pending review] | src/compiler/Compiler.cpp |
| 2026-09-24 | **A name a unit declares and an import also binds is a compile error**, not a silent shadow: which of the two wins would otherwise depend on the compiler's lookup order, and a program's meaning must not rest on that. Scala reports the ambiguity too. Not in the plan | [agent, pending review] | src/compiler/Compiler.cpp, docs/STATUS.md |
| 2026-09-24 | **`ExecutionEngine::callNative` gained the last-resort `catch (...)` clause.** The plan's audit concluded the engine needed only the two pass-through clauses, and it already had them — but a native from a `dlopen`'d plug-in that throws something which is not a `std::exception` at all escaped the VM and **terminated the process**. `std::logic_error` still reaches the `std::exception` arm and is re-thrown untouched, so D74 survives. A fixture drives it through the stand-in provider, and removing the clause turns that fixture red | [agent, pending review] | src/runtime/ExecutionEngine.cpp, tests/conformance/25-interop/ |
| 2026-09-24 | **`ImportError` is added to the prelude but is NOT catchable from protoScala.** A0-14's premise ("a failed import must be a catchable Throwable") was written before A0-1 settled on compile-time resolution; with imports resolved when the file is compiled, a failed import is a **compile error**. The class is kept and made to earn its place at the one boundary where it is observable: `ScalaModuleProvider::tryLoad` translates a module-load failure into `ScalaError("ImportError", …)`, so a caller in another runtime gets a precise class name instead of a generic `RuntimeException` | [agent, pending review] | lib/prelude.scala, src/umd/ScalaModuleProvider.cpp |
| 2026-09-24 | **`--version` reports the prelude path, the discovered provider plug-ins and the routed prefixes.** The plan asked for one line per *loaded* plug-in, which needs a `Session`, which needs the prelude. Discovery plus an ABI probe needs no `ProtoSpace`, so `--version` stays cheap and says "discovered", which is not the same as "loaded" and the output says which | [agent, pending review] | src/main.cpp, src/umd/ProviderPlugins.cpp |
| 2026-09-24 | **`find_library(NAMES protoST)` finds nothing**: protoST builds `libprotost_runtime.a` and `libprotost_frontend.a`, two static libraries, not one shared object. The plan's Task 10 snippet would have silently skipped | [agent, pending review] | tests/unit/CMakeLists.txt |
| 2026-09-24 | **`umd/protost-interop` is OFF by default** (`-DPROTOSCALA_PROTOST_INTEROP=ON`). It links a sibling repository's build output, so leaving it on makes this suite depend on another tree's build state — and a sibling agent was rebuilding protoST while this ran. It was **built and run**, and its outcome is recorded under R5 in STATUS with the date and the protoST commit; the end-to-end case is kept `DISABLED` in its failing shape rather than deleted or weakened | [agent, pending review] | tests/unit/CMakeLists.txt, CMakeLists.txt, docs/STATUS.md |
| 2026-09-24 | **`module-runs-once.scala` was rewritten because it could not fail.** It printed `Setup.ready`, and a module loaded twice still answers `true`. It now compares two imports with `eq`, which a second load cannot satisfy. Confirmed by disabling the exactly-once cache and watching it go red — the same check the plan asks of every fixture, applied to a fixture the plan itself specified | [agent, pending review] | tests/conformance/24-modules/module-runs-once.scala |
| 2026-09-24 | **The prelude-image test compares every constant field, not only `disassemble()`.** Found by breaking the reconstruction on purpose: `disassemble()` is a listing, not a dump, so dropping a send site's D5 fallback or a `SuperSite`'s `exact` flag left the two listings equal. A self-retiring test also states that the current prelude exercises neither field, so the absence cannot be mistaken for coverage | [agent, pending review] | tests/unit/test_prelude_image.cpp |
| 2026-09-24 | **The README's polyglot-interop claim was rewritten.** The opening bullet promised, in the present tense, "import modules from sibling runtimes (`import py.numpy as np`) through protoCore's Unified Module Discovery, with no serialization at the boundary", and the subtitle promised "transparent in-memory interop with Python, JavaScript, Smalltalk and Clojure". Phase 6 does not deliver that, and the project is about to be announced to an audience that will try exactly that line. Both now state what ships, what routes, and what is designed but not yet reachable | [agent, pending review] | README.md, docs/INTEROP.md, docs/tutorial/ |

## Track Y — a working cross-runtime import (2026-09-24)

| Date | Decision | Taken by | Where |
|---|---|---|---|
| 2026-09-24 | **The Phase 6 conclusion that this needed a protoCore change was overturned, and no protoCore change was made.** Phase 6 recorded the mechanism correctly — `ModuleProvider::tryLoad(path, ctx)` receives the caller's context, and every provider resolved its runtime from `ctx->space` — and concluded that serving a caller in another `ProtoSpace` required a change to the UMD contract. It did not: a `ModuleProvider` is an object with its own state, so a provider takes its runtime from that state and uses `ctx` only to allocate the result in the caller's context, which is what the argument is for. protoCore is untouched | [agent, pending review] | protoST `e82682b`, docs/STATUS.md R5, docs/INTEROP.md §6 |
| 2026-09-24 | **protoST's provider takes its runtime from the space registry's single entry (`soleSTRuntime`) rather than from a pointer captured at construction.** The maintainer's analysis proposed capturing the runtime at construction or registration. The provider is registered once per process under a `std::call_once` and protoCore's `ProviderRegistry` never releases it, so it outlives every `STRuntime` — and a captured pointer would dangle for any host that builds a runtime per test, which protoST's own suite does. Reading the registry gives the same "my own runtime" answer with a correct lifetime, because `STRuntime`'s constructor and destructor already maintain it. Two registered runtimes make the choice ambiguous and are refused rather than resolved arbitrarily | [agent, pending review] | protoST src/modules/STModuleProvider.cpp |
| 2026-09-24 | **A cross-runtime load runs in protoST's own space, not the caller's.** Running a module's top level on the caller's context would allocate protoST objects in the caller's heap and intern the module's literals in the caller's symbol table, leaving protoST's own later lookups of those names missing — the per-space interning trap T5-a already hit in `prim_Import_from`. The caller's context is used for the result only | [agent, pending review] | protoST src/modules/STModuleProvider.cpp |
| 2026-09-24 | **The module namespace is rebuilt with keys interned in the caller's space; the values are not touched.** An attribute key is the address of an interned symbol and protoCore interns per `ProtoSpace`, so a member name needing a heap symbol has a different address in each space. Because protoCore embeds a short string in the pointer word, `value` (5 bytes) matched across spaces by accident and `Counter` (7 bytes) missed **silently**; the tests use a 7-byte name for that reason and compare the two symbol addresses so they cannot pass by accident. Only the mapping is rebuilt, which is what keeps the no-copy property true | [agent, pending review] | protoST src/modules/STModuleProvider.cpp, docs/INTEROP.md §6 |
| 2026-09-24 | **An import from a thread other than the one that constructed the protoST runtime is refused with a message.** A foreign caller has no context of its own in protoST's space, so the load runs on protoST's root context, and only the constructing thread may allocate on it (protoST D26). `STRuntime::isOwnerThread()` was added for the check. A silent race here would be the worst possible outcome, so the unsupported case is loud | [agent, pending review] | protoST include/protoST/STRuntime.h, src/modules/STModuleProvider.cpp |
| 2026-09-24 | **`PROTOSCALA_PROTOST_INTEROP` now defaults to ON** (it was OFF, recorded in the Phase 6 entry above). `import st.<module>` works, and `umd/protost-interop` is the only place it is tested: an interop claim nobody runs is a claim nobody keeps. It still links nothing of a sibling repository into the shipped binary, it is built only when protoST is actually found beside this tree, and `-DPROTOSCALA_PROTOST_INTEROP=OFF` restores a suite that refers to no other tree. The trade accepted: protoScala's default suite now depends on protoST's `build_release` being present and current | [agent, pending review] | CMakeLists.txt, docs/STATUS.md |
| 2026-09-24 | **The Phase 6 "diagnosis" test was turned round rather than deleted**, and the `DISABLED` end-to-end test was enabled unchanged apart from its directive. The diagnosis test asserted the defect (`EXPECT_EQ(inOtherSpace, PROTO_NONE)`), so it had to change or fail; it now asserts the contract in both spaces, and keeps the history in its comment | [agent, pending review] | tests/unit/protost_interop.cpp |
| 2026-09-24 | **Both `py` fixtures stay `XFAIL`, with directives that name measured blockers.** `foreign-python-keyword.scala` cannot be converted at all: protoPython ships no numpy (213 entries in `lib/python3.14/`, none of them numpy), and a stand-in dressed as numpy is the failure mode this family already rejected. `foreign-python-open-encoding.scala` is blocked on ownership rather than content: `PythonEnvironment::fromContext` ignores its `ctx` and returns a `thread_local`, and `PythonEnvironment::getProcessSpace` is a function-local static ProtoSpace, "one per process" by design, which a co-resident `Session` contradicts. Those are `PythonEnvironment`'s model — R5's question — not a provider-local change. Nothing was stubbed and no expectation was weakened | [agent, pending review] | tests/conformance/23-named-arguments/, docs/INTEROP.md §6.1 |

## Track F — file input and output (2026-09-25)

The maintainer ruled the shape: **be faithful to Scala when reading**, because
`scala.io.Source` is real Scala standard library, and verify against real `scalac`
where the semantics are checkable; **do not simulate `PrintWriter` when writing**,
because protoScala has no Java interop and will not have one, so provide a minimal
explicit surface and document it as a deviation caused by the absence of Java.
Everything below is a decision taken by the implementing agent under the standing
rules — follow Scala by default, bounded by proportionality; the platform is
late-binding, and late binding excuses late detection, never silent failure.

`scalac 3.9.0` (`tools/scala3-3.9.0` in the workspace) was the reference. Each row
that says "verified" means a program was compiled and run on the JVM and its output
compared.

| Date | Decision | Taken by | Where |
|---|---|---|---|
| 2026-09-25 | **The reading surface is `Source.fromFile`, `Source.fromFile(path, enc)`, `Source.fromString`, `mkString`, `getLines()`, `close()`, `isOpen` and `toString`, and nothing else of `scala.io.Source`.** What is left out is the part of Scala's surface that exists because a `Source` is an `Iterator[Char]`: `next`, `hasNext`, the character-wise `toList`, `take`, `drop`, `foreach`, `size`, `Position`, `reportError`, `withPositioning`, `getLines` on a `Source` built from a URL or an InputStream. Each needs an `Iterator`, which the dialect does not have; none is what a program reading a file reaches for. `isOpen` is not Scala's and is added because `close()` is observable (D101) and a program should be able to ask | [agent, pending review] | lib/prelude.scala, docs/STATUS.md D100 |
| 2026-09-25 | **`getLines()` answers a `List[String]` (D100).** Scala answers an `Iterator[String]`. There is no `Iterator` in the dialect and inventing one for this is a collection-library decision, not a file-I/O one. The consequence a Scala programmer notices is benign: `getLines().toList` still compiles and still means the same thing, because `toList` on a `List` is the identity. The consequence that is not benign is stated rather than hidden — nothing streams, so a file larger than memory cannot be processed | [agent, pending review] | lib/prelude.scala, docs/STATUS.md D100 |
| 2026-09-25 | **A `Source` is read once, at `fromFile`, and may be read again (D101).** Verified against scalac: `val s = Source.fromFile(f); (s.mkString, s.getLines())` answers `(17, List())` on the JVM — the second read is empty, because the iterator is exhausted. protoScala answers `(17, List(alpha, beta, gamma))`. Cost of matching Scala: a cursor and a consumed-ness flag, to reproduce a behaviour that is a reliable source of bugs. Strictly more programs work this way and only a program relying on exhaustion can tell. The divergence is pinned by a fixture that states both answers | [agent, pending review] | lib/prelude.scala, tests/conformance/26-file-io/a-source-can-be-read-twice.scala |
| 2026-09-25 | **`close()` is not a no-op, even though nothing is held open.** It would have been the simpler choice, and it is refused: a program that reads a source it has already closed has a bug, and the platform's rule is that late binding excuses late detection and never silent success. Scala raises `IOException: Stream Closed`; protoScala raises `IOException` with `<origin> (Stream Closed)`, naming the path, because the maintainer's requirement that every message name the path outranks matching a message that does not | [agent, pending review] | lib/prelude.scala, tests/conformance/26-file-io/a-closed-source-cannot-be-read.scala |
| 2026-09-25 | **The class of a failure follows the JVM's rule: every failure of `open` is a `FileNotFoundException`, and every failure after the descriptor exists is an `IOException` (D97).** This looks wrong until it is measured, and it was measured: scalac reports a missing file, a directory, AND a file with no read permission all as `java.io.FileNotFoundException`. One rule covers reading and writing, so the two halves of the track do not disagree with each other. `IOException` is placed under `Exception`, not `RuntimeException`, because on the JVM an I/O failure is checked and `catch case e: IOException` is what a Scala programmer writes | [agent, pending review] | src/runtime/FilePrimitives.cpp, lib/prelude.scala, docs/STATUS.md D97 |
| 2026-09-25 | **The reason in a failure message is spelled in English from an errno table, not taken from `strerror` (D98).** The JVM's message is `<path> (<strerror>)`, and `strerror` is localised: on this machine the JVM reports `sample.log (No existe el archivo o el directorio)`. A fixture cannot pin that, and a runtime whose diagnostics change language with `LANG` is worse for a reader than one that does not. The table covers every errno these operations can produce; anything outside it keeps `std::strerror` and is documented as locale-dependent | [agent, pending review] | src/runtime/FilePrimitives.cpp, docs/STATUS.md D98 |
| 2026-09-25 | **`MalformedInputException`'s message names the path and the byte offset**, where Scala's says only `Input length = 1` (verified). The class matches; the message deliberately does not. A message that does not name the file is of no use to a program that read several, and the maintainer's brief requires every message to name the path | [agent, pending review] | src/runtime/FilePrimitives.cpp, docs/STATUS.md D98 |
| 2026-09-25 | **UTF-8 decoding is strict, and an unimplemented charset is refused rather than decoded as UTF-8 (D99).** Verified that scalac rejects an overlong form (`C0 AF`), a surrogate (`ED A0 80`) and a value above U+10FFFF (`F5 80 80 80`) with `MalformedInputException`, so strictness matches rather than exceeds the JVM. `Source.fromFile(path, enc)` accepts the encoding argument so the common Scala spelling compiles, and refuses any name but UTF-8 with an `UnsupportedOperationException`: `ISO-8859-1` is a charset the JVM supports, and quietly decoding its bytes as UTF-8 would hand the program plausible nonsense. A charset table is real machinery for a case only unusual code needs | [agent, pending review] | src/runtime/FilePrimitives.cpp, docs/STATUS.md D99 |
| 2026-09-25 | **The writing surface is named `FileIO`, with exactly the four operations the maintainer specified.** The names considered and rejected: `Files`, which is `java.nio.file.Files` and would suggest that surface exists; `File`, which is `java.io.File` and whose `exists`/`delete` are instance methods, so the same names would mean something different; `Out` and `IO`, which say too little. `FileIO` is not a Scala or Java name, so nothing is being imitated, and it reads as what it is. `write`/`append`/`exists`/`delete` are the names Python and Node users already have for these four operations, which is who will look for them first | [agent, pending review] | lib/prelude.scala, docs/STATUS.md D102 |
| 2026-09-25 | **`FileIO.delete` answers `false` only for "there was nothing there", and raises for every other failure.** `java.io.File.delete()` answers `false` for both, which is precisely the silent swallow this track exists to avoid: a permission denial reported as `false` leaves the program believing the path is clear. A directory is refused rather than removed, which is also what keeps the primitive safe for a test suite to exercise | [agent, pending review] | src/runtime/FilePrimitives.cpp, tests/conformance/26-file-io/delete-refuses-a-directory.scala |
| 2026-09-25 | **A path containing a NUL byte is refused with an `IllegalArgumentException`.** A protoScala `String` can hold one; a POSIX path cannot. `c_str()` would stop at the NUL and the syscall would operate on the prefix — a **different** file than the program named. This is the one failure in the track that could do damage rather than merely report it, so it is checked before every operation. Scala reaches the same outcome by a different route (the JVM rejects NUL in a path); the class is not the one the JVM raises, and naming the real problem was preferred | [agent, pending review] | src/runtime/FilePrimitives.cpp, tests/conformance/26-file-io/a-nul-in-a-path-is-refused.scala |
| 2026-09-25 | **The public surface is written in protoScala, on top of five hidden natives.** `__fileReadText`, `__fileWriteText`, `__fileExists`, `__fileDelete` and `__splitLines` are the only new C++; `Source`, `BufferedSource` and `FileIO` are prelude classes, following the `Try` / `__tryOf` pattern already in the tree. Every decision above the syscall — what `getLines()` answers, whether a closed source may be read — is a language decision and is readable where a reader looks for it. One splitter serves `fromFile` and `fromString`, so the two cannot disagree | [agent, pending review] | src/runtime/FilePrimitives.cpp, lib/prelude.scala |
| 2026-09-25 | **`close` is called explicitly and its result checked on the write path.** On some filesystems a write error is reported for the first time at `close`, so a `close` whose result is dropped turns a failed write into a successful-looking one. The read path ignores it, where there is nothing left to lose. A short `write` loops; a regular-file `write` never returns short, so that loop is **not** exercised by any fixture, and the report says so rather than implying coverage it does not have | [agent, pending review] | src/runtime/FilePrimitives.cpp |
| 2026-09-25 | **The worked example takes the log's path as `args(0)` (default `"sample.log"`) and the fan width as `args(1)`.** A data path is resolved against the working directory, here exactly as on the JVM: protoScala has no notion of "the directory the script is in" because Scala has none, and inventing one would be a new language feature outside this track. So the documented invocation changes to `cd examples/log-report && protoscala Main.scala`, and `tests/cli/examples.sh` runs both that form and the named-path form from the repository root and from `/`. The alternative — keeping the embedded text as a fallback when the file is missing — was rejected: a fallback is how the duplication got there | [agent, pending review] | examples/log-report/Main.scala, tests/cli/examples.sh, docs/tutorial/worked-example.md |
| 2026-09-25 | **The diff that guarded the duplicate log is replaced by a check that the program reads the file it is given**, not simply deleted. `tests/cli/examples.sh` copies `sample.log`, appends one more `ERROR` line to the copy and demands a different report (`Error 4`). Confirmed red by putting the embedded text back on purpose. Removing a workaround is not finished until the test that guarded it has been replaced by a test of the real thing | [agent, pending review] | tests/cli/examples.sh |
| 2026-09-25 | **The conformance runner honours `PROTOSCALA_RUN_CWD`.** A tutorial snippet must be copy-pasteable, so it has to name `"notes.txt"` rather than a path assembled from an environment variable; a relative path is resolved against the working directory, and the runner's working directory is the fixture's own **source** directory, which no test may write into. So CMake creates a private directory per file-I/O tutorial fixture inside the build tree and the runner starts the program there. Unset, the runner behaves exactly as before, down to naming the script relatively — several fixtures match on `<file>:<line>: error:`, and an absolute path would have changed that silently. The `26-file-io` fixtures keep the other mechanism (an explicit `PROTOSCALA_TEST_TMP`, and a refusal to run when it is unset) because they test the mechanism rather than read as prose | [agent, pending review] | tests/conformance/run.sh, tests/CMakeLists.txt |
| 2026-09-25 | **No C++ unit test was added.** The two pieces with interesting edges — the line splitter and the UTF-8 validator — are in an anonymous namespace in `FilePrimitives.cpp`, and exporting them to test them would widen a header for the test's benefit. Both are instead driven end to end: the splitter by nine fixtures covering every row of the table verified against scalac, the validator by committed files of exactly the bytes each malformed form needs. Four mutations of the validator's three strictness checks were run to confirm the fixture distinguishes them | [agent, pending review] | tests/conformance/26-file-io/ |
| 2026-09-25 | **The README's stale "not implemented yet" paragraph was rewritten.** It said "Exceptions, `enum`, extension methods, the Phase 3 collections, string interpolation and UMD are not implemented yet" — every one of which has shipped, some of them four phases ago. It sat immediately below the feature list this track was amending, in a README about to be shown to an audience that will read it as current. It now names the real absences | [agent, pending review] | README.md |
| 2026-09-25 | **An uncaught failure raised inside a prelude method reports the prelude's line number against the user's file name.** Found by Track F, pre-existing, not fixed. `protoscala examples/log-report/Main.scala` with no log prints `Main.scala:240: …`, where 240 is `lib/prelude.scala`'s line: the **name** comes from the outermost compilation unit and the **line** from the innermost VM frame. The class, the path and the reason are all correct; only the prefix misleads. The same mismatch has always applied to a failure raised inside an imported module, so the diagnostic did not change — Track F made it **common**, because `Source.fromFile` failing is the ordinary case. Fixing it changes what D14's prefix reports for every cross-unit frame, which is a public surface and outside this brief. Recorded in STATUS under "Known issues"; the fixtures that pin these failures match on the message and not on the prefix, deliberately | [agent, pending review] | docs/STATUS.md "Known issues" |

---

## Phase P3 — global interning and the module list as a GC root (2026-09-24/25)

The maintainer's ruling of 2026-09-24: *"hacer la internación global y la lista de
módulos como raíz"*, and, separately, that a module's identity in that list is
**provider + path + version**. The full argument for every row is in
`docs/plans/2026-09-24-phase-p3-global-interning.md` (Task 0) and in
`docs/platform/GLOBAL-INTERNING-SPEC.md` §7.

| Date | Decision | Taken by | Where |
|---|---|---|---|
| 2026-09-24 | **Interning becomes process-global, and the module list becomes a GC root.** An attribute key is the address of an interned symbol, so a per-space table made two spaces of one process disagree about the key for the same name — except for names inside `INLINE_STRING_MAX_BYTES`, which protoCore embeds in the pointer word and which therefore matched by accident. A 5-byte name resolved and a 7-byte one missed, returning `PROTO_NONE`, which is also a legitimate value | maintainer | GLOBAL-INTERNING-SPEC §1.1, §2.1 |
| 2026-09-24 | **A module's identity in the global list is provider + path + version, not path alone.** Path alone aliased silently: `provider:st/counter_lib` and a local `counter_lib` collapsed into one module with the first load winning for both. Modules are process-level and perennial, so two coexisting versions are two modules for the life of the process | maintainer | GLOBAL-INTERNING-SPEC §2.2, plan D11 |
| 2026-09-25 | **Tuple interning stays per-space — an explicit deviation from the literal ruling "all interning should be global".** Intern globally what is keyed by **content**; keep per-space what is keyed by **address**. A symbol's key is its bytes, which are space-independent, so a global table delivers the cross-space identity the ruling exists for. `TupleInterner`'s key is the slot *pointers*, so a global tuple table would deliver no cross-space identity in the ordinary case at all — and the one case where it *would* alias (a tuple of now-globally-interned symbols built in two spaces) would return one node whose cells live in whichever space created it first: one collector tracing another's heap, elements kept alive by a foreign collector, for zero benefit. It is not a use-after-free today only because `~ProtoSpace` never frees its cell heap, and a design that is safe because of a leak is not a design. `GlobalInterning.TupleInternerStaysPerSpace` stops a future reader from "completing" the change | [coordinator, pending review] | GLOBAL-INTERNING-SPEC §2.4, D5 |
| 2026-09-25 | **protoCore 2.2.0 and `PROTOCORE_ABI_SOVERSION` 2 → 3**, against the plan's recommendation to keep 2. The plan itself gives the deciding reason: unlike P1 and P2, a stale embedder here **links successfully and merely returns wrong answers** — there is no load-time error, no crash and no diagnostic, just a `getAttribute` returning `PROTO_NONE`. A soname bump converts that into a load-time error. It costs one line in each of the five runtimes today, and a great deal later. The `ProtoSpace` layout is byte-for-byte identical, proved by an empty `offsetof` diff, so the bump is a deliberate choice rather than a consequence | [coordinator, pending review] | GLOBAL-INTERNING-SPEC §6.1, D7 |
| 2026-09-25 | **protoPython is migrated to a `ProtoRootSet` in this phase**, and protoCore's stop-the-world module loop is retired only after that lands. Deferring to a P3b was rejected: the pause rule is binding and the kernel already violated it — the O(modules) loop ran inside the pause and was missing from `docs/GarbageCollector.md`'s own cost table. protoPython used `space->moduleRoots` as a general-purpose, *removable*, slot-replacing embedder root set (eight writers, three readers, ~20 prototypes, a thread-roots dict, a ByteBuffer, a bytecode cache, symbols) and erased from it in `~PythonEnvironment`. The append-only module table is not a substitute, so deleting the loop first would have silently dropped protoPython's GC roots — a failure its passing tests cannot reach | [coordinator, pending review] | GLOBAL-INTERNING-SPEC §6.2, D12 |
| 2026-09-25 | **The global module table supplements per-space roots; it does not replace them.** Each entry carries the owning `ProtoSpace` and the mark walk visits only the collecting space's entries: the table is global, the tracing is not. A single global root set replacing per-space roots was rejected — it would kill the per-space prototype roots, which must stay per space, and make every collector traverse every other space's live graph on every cycle | [agent, pending review] | GLOBAL-INTERNING-SPEC D1 |
| 2026-09-25 | **`ProtoSpace::symbolTable` keeps its type and slot and points at the one global table.** All eleven existing `ctx->space->symbolTable` call sites are untouched, the layout is unchanged, and the mid-construction sentinel that six null checks in `ProtoObject.cpp` rely on still fires exactly when it used to — the field is null before the constructor reaches it and non-null after, per space, regardless of what other spaces have done | [agent, pending review] | GLOBAL-INTERNING-SPEC D2 |
| 2026-09-25 | **A `SymbolTable` shard mutex is a strict leaf lock**, never ordered against `ProtoSpace::globalMutex`, and no Cell is allocated while it is held. Stated in the source, because with one table serving several spaces breaking it would deadlock two collectors instead of one. The read path is allocation-free by construction and a test pins it | [agent, pending review] | GLOBAL-INTERNING-SPEC D3 |
| 2026-09-25 | **`~ProtoSpace` stops freeing the symbol table, and only the symbol table.** This fixes a leak rather than creating one: `~SymbolTable` frees only the `Bucket` nodes, so every destroyed space already leaked its whole symbol set. `delete tupleInterner` stays | [agent, pending review] | GLOBAL-INTERNING-SPEC D4 |
| 2026-09-25 | **The pause-cost property is gated by two deterministic counter tests, not by a wall-clock assertion.** A pause timing on this hardware is noise. `PROTOCORE_GC_PROFILE=1` measures it and the figure is recorded as evidence for the documentation; `lastCaptureShardReads()` and `stwVisitViolations()` are what gate it | [agent, pending review] | GLOBAL-INTERNING-SPEC §3, D6 |
| 2026-09-25 | **protoST's `buildCallerFacade` is not simplified.** Its key rebuild is redundant after P3, but it also re-parents the namespace to the caller's `objectPrototype`, and **prototypes stay per space**. Deleting it would trade a silent attribute miss for a silent prototype mismatch. Only its comment is corrected | [agent, pending review] | GLOBAL-INTERNING-SPEC D9 |
| 2026-09-25 | **`Session::loadForeign` publishes through `ProtoSpace::registerModule`**, and its local cache is keyed by the kernel's identity so the two cannot disagree. Before P3 this path reached neither `SharedModuleCache` nor any `moduleRoots`, so the module's only anchor was inside the providing runtime; destroying that runtime while this Session still held its values dropped it, and the tests survived only because construction order happened to make the runtime destroyed last | [agent, pending review] | GLOBAL-INTERNING-SPEC D10, D13 |
| 2026-09-25 | **`getImportModuleImpl`'s two wrapper-construction branches are unified with the `addParent(objectPrototype)` that only the cache-hit branch had.** Before P3 the same call returned a wrapper with a different prototype chain depending on whether the module happened to be cached already, which is nondeterminism in a kernel path. The plan asserted the two branches were identical; they were not, and reality was followed | [agent, pending review] | GLOBAL-INTERNING-SPEC D14 |
| 2026-09-25 | **`stand-in-provider-long-keyword.scala` is neither re-pointed nor retired.** The plan said its purpose was the ">6-byte name crosses a space" trap and that P3 would make it pass for the wrong reason. Measured: its purpose is the `createSymbol`-versus-`fromUTF8String` convention for a keyword-parameter key **within one space** (`tests/unit/probe_provider.cpp`, `keywordNamed`), which P3 does not change and does not make vacuous. Its guard was re-verified live by mutation and its comment now states exactly what it guards after P3. Reality was followed over the plan | [agent, pending review] | GLOBAL-INTERNING-SPEC §6.4 |
| 2026-09-25 | **`ModuleRootTable::purgeSpace`, called from `~ProtoSpace`** — not in the plan, found by a test during execution. The table is append-only, so an entry outlives the `ProtoSpace` it names, and the allocator can hand a later `ProtoSpace` the same address; that space's collector would then match the dead space's entries by owner and trace cells in a heap with no owner. It is not a crash today only because `~ProtoSpace` never frees its cell blocks — the same "safe because of a leak" shape that made the plan decline a global tuple interner, and mechanism (iii) that D5 listed as its cost. Tombstones in place (the chunks must not move, or the concurrent walk breaks), after the space's GC thread is joined; `Entry::owner` becomes atomic because a tombstone races another space's mark reading that word; `countOwnedBy` added because `forEachCaptured` dereferences its space argument | [agent, pending review] | GLOBAL-INTERNING-SPEC D15 |
| 2026-09-25 | **Three resident-memory assertions skip under AddressSanitizer.** An RSS bound measures the sanitizer, not the code — 200k `createSymbol` calls commit ~0 KB in Release and 26 MB under ASan. Two of the three pre-date P3 and were already red in an ASan build, so this makes protoCore's ASan suite meaningful rather than hiding a P3 defect; the Release build still asserts them | [agent, pending review] | GLOBAL-INTERNING-SPEC D16 |
| 2026-09-25 | **Three P3 tests were strengthened after their own mutation failed to turn them red**, recorded rather than quietly fixed: the perennial-symbol case interned in a long-lived context; the second-space case asserted only a count that a per-space table leaves unchanged; the concurrency case let its mutation through in 1 of 3 runs. A mutation that is only sometimes caught is only sometimes a test | [agent, pending review] | GLOBAL-INTERNING-SPEC D17 |
